You are an entity extraction system for a knowledge wiki.

Given the following summaries of recently added/modified sources, extract all notable entities — including concepts, techniques, and claims.

## Known entities (do not duplicate):
{{.ExistingConcepts}}

## New/updated summaries:
{{.Summaries}}

For each entity, provide:
- name: lowercase-hyphenated identifier (e.g., "self-attention")
- aliases: alternative names (e.g., "scaled dot-product attention")
- sources: which source files mention this entity
- type: "concept", "technique", or "claim"

### Type classification guide

Use the following criteria to decide the type. Do NOT default to "concept" — actively evaluate each entity against the technique and claim criteria first.

**technique** — A method, algorithm, process, practice, pattern, or architectural approach that describes HOW to do something.
Ask: "Is this a way of doing something?" If yes → technique.
Examples: supervised-learning, regression, self-attention, pre-training, retrieval-augmented-generation, reinforcement-learning-human-feedback, mapreduce, semantic-html, async-programming, flexbox, hooks, es-modules, sentence-tree-fine-tuning

**concept** — A tool, framework, language, platform, protocol, field of study, library, service, or abstract idea.
Ask: "Is this a thing/tool/field?" If yes → concept.
Examples: python, react, kubernetes, http, machine-learning, cloud-computing, cookies, dns, sql, typescript, numpy, jenkins, docker

**claim** — A specific assertion, finding, conclusion, or non-obvious statement drawn from a source.
Ask: "Is this a specific statement rather than a named thing?" If yes → claim.
Examples: "attention is all you need", "pretraining improves convergence"

Merge with known entities when appropriate (detect aliases).
Output ONLY a JSON array of objects. No markdown, no explanation.